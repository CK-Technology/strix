# Moved: S3 Compatibility

This document has been consolidated to avoid drift.

Use the canonical compatibility matrix at:

- `docs/s3-compatibility.md`

For practical client validation steps (AWS CLI, restic, rclone, s3cmd), use:

- `docs/tool-compatibility-testing.md`
