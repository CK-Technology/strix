# Docker Compose for Strix
#
# Usage:
#   cd deploy
#   docker compose up -d          # Start Strix
#   docker compose logs -f strix  # View logs
#   docker compose down -v        # Stop and remove volumes
#
# With monitoring (Prometheus + Grafana):
#   docker compose -f docker-compose.yml -f docker-compose.monitoring.yml --profile monitoring up -d
#
# Access:
#   S3 API:       http://localhost:9000
#   Web Console:  http://localhost:9001
#   Prometheus:   http://localhost:9091 (only with monitoring profile)
#   Grafana:      http://localhost:3000 (only with monitoring profile)
#
# Note: Metrics are NOT exposed to the host by default. The monitoring override
# binds metrics to 0.0.0.0:9090 inside the Docker network only, allowing Prometheus
# to scrape without exposing metrics publicly.

services:
  strix:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    image: strix:latest
    container_name: strix
    restart: unless-stopped
    ports:
      - "${STRIX_PORT_S3:-9000}:9000"
      - "${STRIX_PORT_CONSOLE:-9001}:9001"
      # Metrics NOT exposed by default - bind to loopback inside container
      # To expose metrics, set STRIX_METRICS_ADDRESS=0.0.0.0:9090 and add port mapping
    environment:
      STRIX_ROOT_USER: ${STRIX_ROOT_USER:-admin}
      STRIX_ROOT_PASSWORD: ${STRIX_ROOT_PASSWORD:?STRIX_ROOT_PASSWORD is required}
      STRIX_LOG_LEVEL: ${STRIX_LOG_LEVEL:-info}
      STRIX_LOG_JSON: ${STRIX_LOG_JSON:-false}
      STRIX_JWT_SECRET: ${STRIX_JWT_SECRET:-}
      STRIX_MULTIPART_EXPIRY_HOURS: ${STRIX_MULTIPART_EXPIRY_HOURS:-24}
      STRIX_S3_RATE_LIMIT: ${STRIX_S3_RATE_LIMIT:-1000}
      STRIX_METRICS_ADDRESS: ${STRIX_METRICS_ADDRESS:-127.0.0.1:9090}
      STRIX_OTLP_ENDPOINT: ${STRIX_OTLP_ENDPOINT:-}
      STRIX_SERVICE_NAME: ${STRIX_SERVICE_NAME:-strix}
    volumes:
      - strix-data:/var/lib/strix
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  # Optional: Prometheus for metrics collection
  prometheus:
    image: prom/prometheus:latest
    container_name: strix-prometheus
    profiles: ["monitoring"]
    restart: unless-stopped
    ports:
      - "9091:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.enable-lifecycle'
    depends_on:
      - strix

  # Optional: Grafana for dashboards
  grafana:
    image: grafana/grafana:latest
    container_name: strix-grafana
    profiles: ["monitoring"]
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: false
    volumes:
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus

volumes:
  strix-data:
  prometheus-data:
  grafana-data:
